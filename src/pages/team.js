import React from "react"

import Layout from "../components/layout"
import SEO from "../components/seo"

const TeamPage = () => (
  <Layout pageTitle="Out team">
    <SEO title="Team page" keywords={[`gatsby`, `application`, `react`]} />
  </Layout>
)

export default TeamPage